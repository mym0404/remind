# Responsive Card Grid

## 문제

카드 목록이 viewport 너비에 맞춰 열 수를 바꾸게 만든다.

## 핵심 개념

- layout은 CSS Grid가 맡는다.
- `auto-fit` 또는 `auto-fill`과 `minmax`로 열 수를 맡긴다.
- 고정 3열은 responsive contract가 아니다.

## 풀이 흐름

카드 wrapper에 `.card-grid`를 둔다. CSS에서 `grid-template-columns`만 responsive 규칙으로 만든다.

## 핵심 코드

```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 220px), 1fr));
  gap: 12px;
}
```

## 테스트가 보는 것

- 카드들이 grid container 안에 렌더링되는지 본다.
- CSS에 intrinsic responsive grid 규칙이 있는지 확인한다.

## 실수 포인트

- 고정 3열 grid는 모바일에서 overflow를 만든다.
- 카드 폭만 정하고 grid column을 안 바꾸면 남는 공간 분배가 어색해진다.
